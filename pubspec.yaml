name: hkd
description: Hatay Kuryeler Derneği Mobil Uygulaması
publish_to: 'none'

version: 1.0.4+5

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  app_links: ^6.3.2
  http: ^1.2.2
  package_info_plus: ^8.0.2
  geolocator: ^13.0.2
  firebase_core: ^3.15.2
  firebase_messaging: ^15.2.7
  shared_preferences: ^2.5.3
  supabase_flutter: ^2.10.3
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
